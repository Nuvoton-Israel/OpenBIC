manifest:
  remotes:
    - name: nuvoton
      url-base: https://github.com/Nuvoton-Israel

  # Please add items below based on alphabetical order.
  # These dependencies may need updating if new features are added to the firmware.
  projects:
    - name: zephyr_nuvoton
      remote: nuvoton
      repo-path: zephyr
      path: zephyr_nuvoton
      revision: d800dbdeb38a230440310ace5a4f8057092920e0
      import:
        name-allowlist:
          - cmsis
          - mcuboot

  self:
    path: openbic

